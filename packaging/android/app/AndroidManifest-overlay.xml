<?xml version="1.0" encoding="utf-8"?>
<!-- Manifest-merger overlay. Dropped into the generated project's debug build-type
     source set (app/src/debug/AndroidManifest.xml); the Android Gradle manifest
     merger combines it with Pixiewood's generated main manifest. It points the
     <application> at our RuntimeApplication subclass and declares the foreground
     ProxyService plus its permissions — keeping the in-process proxy alive when
     the app is backgrounded. No post-hoc XML editing: the merger does the work. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- extractNativeLibs=true: GTK/Pixiewood dlopen() many .so by path, so the
         native libs must be unpacked to disk. Debug defaults to true (works);
         release defaults to false (libs stay in the APK) → dlopen fails and the
         app crashes on launch. Force extraction for both. -->
    <application android:name="space.ampernic.anothertgproxy.ProxyApplication"
                 android:extractNativeLibs="true"
                 tools:replace="android:name,android:extractNativeLibs">
        <service android:name="space.ampernic.anothertgproxy.ProxyService"
                 android:exported="false"
                 android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                      android:value="proxy"/>
        </service>
    </application>
</manifest>
